# src/api

Streamlit web application. Provides a single-page form where a Traffic Control
Supervisor describes a work zone scenario (jurisdiction, speed limit, lane
count, closure type, duration) and receives three downloadable output files:
a PDF plan sheet, an Excel device list, and a Markdown crew narrative.
