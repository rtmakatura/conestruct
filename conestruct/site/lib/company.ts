// Company HQ — the origin used to auto-compute delivery miles for quote
// generation. Lives server-side so the address is geocoded once via Mapbox
// and only the resulting distance crosses the wire to the browser.
export const COMPANY_HQ_ADDRESS = "2295 S Lipan St, Denver, CO 80223";
