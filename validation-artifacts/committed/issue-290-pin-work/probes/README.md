# #290 checkpoint probes, 2026-09-25, base `6fe391e` (= prod healthz)

Every probe here is a read. Nothing wrote to prod except the in-memory state of one
`/sandbox` page per run. No plan was saved, and no Generate was pressed.

| file | what it is | what it shows |
|---|---|---|
| `rb-broadway.json`, `rb-lincoln.json`, `rb-colfax.json` | `POST https://www.conestruct.com/api/road-bearing` with `{lat,lng}`, one call each | **Broadway** 39.73370, −104.98753: way 131232822, `oneway=yes`, bearing **180.49** (southbound = the legal direction). **Lincoln** 39.73370, −104.98597: bearing **359.35** (northbound = legal). **E Colfax** 39.74020, −104.95600: two carriageways, `oneway=yes`, 270.14 and 90.02. |
| `stations.py` → `stations.json` | imports the shipped `build_corridor`, run from the repo root | Station 0 is the pin. E Colfax EB shoulder at 30 mph, workLen 1000: downstream 0–50, work 50–1050, buffer 1050–1250, taper 1250–1300, advance 1300–1600. The first sign is **1,600 ft from the pin, along the sent bearing**. `use_max` moves every station by +50. |
| `overlay.cjs` → `overlay-*-2-corridor.png/.txt`, `overlay-*-log.txt` | Playwright on prod `/sandbox`, 1440 × 1000: picker → manual lat/lng → pick the detected candidate → Save → Shoulder → Confirm → re-open the picker | **Broadway:** the candidate reads "NORTH BROADWAY SOUTHBOUND (PRIMARY, 180°)" and the direction field reads 180. The corridor draws **south** of the pin: work, then buffer and taper, then **advance warning furthest south (near W 8th Ave)**. **E Colfax:** "EAST COLFAX AVENUE WESTBOUND (PRIMARY, 270°)", and the corridor draws **west**, with advance warning furthest west (past York St). In both cases traffic meets the work before its warning signs. |
| `bbox_probe.py`, `radius_probe.py` | scan bbox area per kind, today vs after (straight road, no centerline) | Shoulder and near-intersection: ×1.00. Flagger: ×1.23–1.59 at a 0° bearing, ×1.38–2.26 at 45°. The box is north-up, so a 45° road pays about 3× its real area. |
| `kinds_probe.py` | per-kind zone lengths and layout stations from the shipped tables | the per-kind table in `../checkpoint.md` (c) |
| `anchor_shift_probe.py`, `anchor-shift-run.sh`, `anchor-shift-results.txt` | a pytest plugin that wraps `build_corridor` and moves its anchor so the pin becomes the work's upstream edge | Baseline: 2,115 passed, 2 skipped. Shifted (either size): **4 failed**, 2,111 passed. The 4 are three Lookout Mountain tests and the page-2 caption test. |
| `leaf_proof.py` | a **sketch** of a single-leaf conversion: a straight-road geodesic round trip | Moving the Lakewood fixture pin 1,050 ft along bearing 180 to (39.708421811, −105.0815) and walking back returns the old anchor to within 7.1e-15°. This proves only that the conversion is exact on a straight frame. It does **not** prove that a converted fixture's recorded buckets, audit and PDF come out identical. That is the real single-leaf proof, and it belongs to the build (checkpoint (g), (k)). |
